version: "3.4"
services:
  app:
    image: php:alpine
    volumes:
      - "./:/app"
    working_dir: /app
    command: "php artisan serve --host=localhost --port=8080"
    expose:
      - 80
    environment:
      VIRTUAL_HOST: mychat.mikolaj.dk
      VIRTUAL_PORT: 8080
      LETSENCRYPT_HOST: mychat.mikolaj.dk
      LETSENCRYPT_EMAIL: mikoo1991@yahoo.com
networks:
  default:
    external:
      name: nginx-proxy
